# Variables de entorno para la app
# Rellena con tus valores reales (NO subir .env.local)

NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=anon-key-aqui
# Llave de servicio (solo en servidor, nunca en cliente)
SUPABASE_SERVICE_ROLE_KEY=service-role-key-aqui
# Password DB si lo necesitas para scripts (no requerido para cliente)
SUPABASE_DB_PASSWORD=postgres_password_aqui
